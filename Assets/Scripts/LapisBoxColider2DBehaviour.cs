using System.Collections;
using System.Collections.Generic;
using UnityEngine.UI;
using UnityEngine;

public class LapisBoxColider2DBehaviour : MonoBehaviour
{
    public GameObject _tempText;

    // Start is called before the first frame update
    void Start()
    {
        //  GameObject.Find("Hand");
        //  GameObject _temp = GameObject.Find("Canvas/Text");
        //_tempText = GameObject.Find("Canvas/Text");
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //private void OnCollisionEnter2D(Collider2D collider)
    //{
    //    if (collider.transform.position.x == Input.GetTouch(0).position.x)
    //    {
    //        if (collider.transform.position.y == Input.GetTouch(0).position.y)
    //        {
    //            //  Debug.Log("Clicou no lápis!")
    //            _tempText.name = "Clicou no lápis!";
    //            //  _temp.text = "Clicou no lápis!";

    //        }
    //    }
    //}
}
